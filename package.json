{
	"name": "cloudflare-line-webhook-server",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "pnpm --filter line-webhook-server dev",
		"deploy": "pnpm --filter line-webhook-server deploy",
		"test": "pnpm --filter line-webhook-server test",
		"cf-typegen": "pnpm --filter line-webhook-server cf-typegen"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.7.5",
		"@cloudflare/workers-types": "^4.20250313.0",
		"typescript": "^5.5.2",
		"vitest": "~3.0.7",
		"wrangler": "^4.0.0"
	},
	"packageManager": "pnpm@9.0.0"
}
